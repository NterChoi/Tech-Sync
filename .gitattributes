# 모든 텍스트 파일 줄바꿈을 LF로 통일 (Windows/Mac 혼용 환경)
* text=auto eol=lf

# 바이너리 파일은 변환하지 않음
*.jar binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary

# Windows 배치 파일은 CRLF 유지
*.bat text eol=crlf
*.cmd text eol=crlf
